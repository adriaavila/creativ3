# Implementation Plans

Generated by the improve skill on 2026-08-03. Execute in the order below. Each executor must read its plan fully, preserve the pre-existing dirty-worktree changes described there, honor STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make outbound Ops actions retry-safe | P1 | M | — | DONE |
| 002 | Turn `/ops` into the action-first command center | P1 | L | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- Plan 001 lands first because Plan 002 makes more outbound work discoverable. The command center must not funnel operators into a send flow that can duplicate messages after an ambiguous failure.
- Plan 002 is intentionally one product change: the server projection, exact-record links, homepage simplification, and navigation hierarchy only produce value together.

## Working-tree warning

The plans were written at commit `e220e23` against a dirty worktree. Relevant existing edits include workspace-aware WhatsApp connection lookup, an onboarding-link generator on the current dashboard, tenant conversation summaries, and monotonic WhatsApp delivery statuses. Executors must preserve those changes; they must not reset, checkout, or overwrite the working tree.

## Findings considered and deferred

- Customizable dashboard widgets: rejected for now; the operator has one workflow and a deterministic queue is smaller and more trustworthy.
- A universal persisted tasks table: rejected for now; existing records already contain the states needed for a read-only projection. Add persistence only when assignment or snoozing becomes a real requirement.
- AI ranking or an AI command bar: rejected for now; priority rules are deterministic and auditable.
- Timed conversation snoozing: deferred; first add close/reopen only if inbox-zero remains blocked after the command-center launch.
- Public login throttling: valid security follow-up, but outside this command-center execution. Plan separately before adding more operators or exposing Ops more broadly.
- A Playwright CI suite: deferred until the repo has a seeded test database. Plan 002 leaves focused Node tests for queue classification and exact-record URLs and requires a manual browser smoke check.
